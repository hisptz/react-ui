import React from "react";

export default function CompletenessDataSources() {
  return (
    <div>
      <h3> Completeness of data sources </h3>
      <p>
        {" "}
        The following visualizations outline completeness of data sources
        (datasets) for all the data elements used in calculations of the
        indicator:{" "}
      </p>

      {/* task to be done here */}
      <p>
        Visualization, showing: Columns: Data sets cross Sub columns: Last
        reporting periods of the datasets(ie. Monthly, quarterly, etc..) Rows:
        User Sub-orgunits Legends: 0-60 low, 60 - 80 average, 80 - 100 high
      </p>
    </div>
  );
}
